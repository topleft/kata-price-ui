// import prices from '../mockData/prices.json';

class PricesApi {
  static getPrices() {
    return new Promise((resolve, reject) => {
      // resolve(prices);
      resolve();
    })
  }
}

export default PricesApi;
